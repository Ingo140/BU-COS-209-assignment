//TERNARY OPERATOR
// The ternary operator is a shorter way of writing an if...else statement I javascript. 
//It is called ternary because it is divided into three parts: a condition, a value if true, and a value if false.

let age = 18;
let status = age >= 18 ? "Adult" : "Minor";

console.log(status);
